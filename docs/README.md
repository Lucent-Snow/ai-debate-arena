# Docs

本目录用于记录这个项目在实现前必须先明确的工程事实，避免一边写一边改口径。

当前优先级：

1. 确认真实 LLM 接口契约
2. 确认默认模型名
3. 明确测试必须走真实接口，不使用 mock
4. 固化 v1 范围和交付边界

文档说明：

- `docs/architecture.md`：项目模块划分、数据流、事件流、实现边界
- `docs/testing.md`：真实接口测试原则、所需环境变量、验收口径
- 需要新增设计决策时，继续在 `docs/` 下追加独立文档，不把结论散落在聊天记录里
